name: Tests

on:
  push:
    branches:
      - "*"

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          yarn
          yarn build
  test-match-entities:
    runs-on: ubuntu-latest
    needs: [install]
    steps:
      - uses: actions/checkout@v2
      - run: |
          ls
          ls node_modules/ts-jest
          cd packages/match-entities
          yarn test
  csv-sample:
    runs-on: ubuntu-latest
    needs: [install]
    steps:
      - uses: actions/checkout@v2
      - run: |
          cd packages/csv-sample
          yarn test
  csv-anonymify:
    runs-on: ubuntu-latest
    needs: [install]
    steps:
      - uses: actions/checkout@v2
      - run: |
          cd packages/csv-anonymify
          yarn test
