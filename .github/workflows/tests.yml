name: Tests

on:
  push:
    branches:
      - "*"

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: install
        run: |
          yarn
          yarn build
      - name: test
        run: |
          yarn turbo run test
